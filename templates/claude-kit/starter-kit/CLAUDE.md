@AGENTS.md

## Claude Code notes

- Workspace commands and checks live in `mobile/CLAUDE.md`, `api/CLAUDE.md` and `jobs/CLAUDE.md`. Read the one for the workspace you touch.
- Settled decisions are in `docs/decisions.md`. Do not reopen them unless the user asks.
- Workflow shortcuts: `/plan <task>` → write the prompt, `/check` → run checks, `/ship` → branch, commit, PR.
- One task per session. Suggest `/clear` when a task is done.
